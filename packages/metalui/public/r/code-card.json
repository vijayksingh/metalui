{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "code-card",
  "type": "registry:ui",
  "title": "Code card",
  "description": "Code as a glass object: numbered, tinted lines (at most 18) on a dark screen under a CODE · LANG · N LINES tag.",
  "dependencies": [],
  "registryDependencies": [
    "https://metalui.dev/r/tokens.json"
  ],
  "files": [
    {
      "path": "packages/metalui/src/blocks/code-card/code-card.tsx",
      "type": "registry:ui",
      "target": "components/metalui/blocks/code-card/code-card.tsx",
      "content": "'use client';\n\nimport * as React from 'react';\nimport { GlassFace } from '../../components/glass-face/glass-face';\nimport { Chip } from '../../components/chip/chip';\nimport './code-card.css';\n\n/* ─────────────────────────────────────────────────────────\n * CODE CARD (the reference design's .codeobj): a custom block\n *   GlassFace › Screen (the code-card recipe) › Chip(glass, LED code) CODE · LANG · N LINES + numbered, tinted lines\n * Custom because the tinted code is drawn by no component. At most 18 lines show; the tag counts all.\n * ───────────────────────────────────────────────────────── */\n\nconst KEYWORDS = /\\b(func|let|var|if|else|return|for|in|while|const|function|import|export|from|class|struct|enum|case|switch|guard|def|async|await|new|true|false|nil|null|self|this)\\b/g;\nconst esc = (s: string) => s.replace(/[&<>\"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' })[c]!);\n\n/** The reference tinting, on escaped text: strings, a trailing comment, keywords, types, numbers. */\nexport function tintCode(code: string, maxLines = 18) {\n  return code\n    .split('\\n')\n    .slice(0, maxLines)\n    .map((l, i) => {\n      const h = esc(l)\n        .replace(/(&quot;.*?&quot;|&#39;.*?&#39;)/g, '<span class=\"mu-code-st\">$1</span>')\n        .replace(/(\\/\\/.*$|#.*$)/, '<span class=\"mu-code-cm\">$1</span>')\n        .replace(KEYWORDS, '<span class=\"mu-code-kw\">$1</span>')\n        .replace(/\\b([A-Z][A-Za-z0-9]+)\\b/g, '<span class=\"mu-code-ty\">$1</span>')\n        .replace(/(?<![\\w#])(\\d+(?:\\.\\d+)?)\\b/g, '<span class=\"mu-code-nu\">$1</span>');\n      return `<span class=\"mu-code-ln\">${i + 1}</span>${h}`;\n    })\n    .join('\\n');\n}\n\nexport interface CodeCardProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'children'> {\n  code: string;\n  /** The language, shown in the tag: \"swift\". */\n  lang?: string;\n  /** Lines shown (the tag counts all). Default 18. */\n  maxLines?: number;\n  /** The tag's text. Default: CODE · LANG · N LINES. */\n  tag?: string;\n}\n\n/** Code as a glass object: numbered, tinted lines under a tag. */\nexport const CodeCard = React.forwardRef<HTMLDivElement, CodeCardProps>(function CodeCard({ code, lang, maxLines = 18, tag, className, ...props }, ref) {\n  const n = code.split('\\n').length;\n  const label = tag ?? `CODE${lang ? ' · ' + lang.toUpperCase() : ''} · ${n} ${n === 1 ? 'LINE' : 'LINES'}`;\n  return (\n    <GlassFace.Root ref={ref} className={className ? `mu-codecard ${className}` : 'mu-codecard'} {...props}>\n      <GlassFace.Screen className=\"mu-codecard-screen\">\n        <Chip variant=\"glass\" className=\"mu-codecard-tag\">\n          <Chip.Lead led=\"code\" />\n          <Chip.Text>{label}</Chip.Text>\n        </Chip>\n        <pre className=\"mu-codecard-code\" dangerouslySetInnerHTML={{ __html: tintCode(code, maxLines) }} />\n      </GlassFace.Screen>\n    </GlassFace.Root>\n  );\n});\n"
    },
    {
      "path": "packages/metalui/src/blocks/code-card/code-card.css",
      "type": "registry:file",
      "target": "components/metalui/blocks/code-card/code-card.css",
      "content": "@import \"../../components/tokens.css\";\n@import \"../../components/glass-face/glass-face.css\";\n\n/* Code card: a custom block. The screen and the tinted code are the code-card recipe (--mu-r-code-card-*);\n   the bezel and glare are GlassFace, the tag Chip. */\n.mu-codecard { min-width: var(--mu-r-code-card-self-min-width); max-width: var(--mu-r-code-card-self-max-width); }\n.mu-codecard-screen { padding: var(--mu-r-code-card-screen-pad-top) var(--mu-r-code-card-screen-pad-x) var(--mu-r-code-card-screen-pad-bottom); background: var(--mu-r-code-card-screen-background); }\n.mu-codecard-tag { position: absolute; left: var(--mu-r-code-card-chip-inset); top: var(--mu-r-code-card-chip-inset); }\n.mu-codecard-code {\n  margin: var(--mu-zero);\n  overflow: hidden;\n  white-space: pre;\n  font: var(--mu-r-code-card-code-font);\n  letter-spacing: var(--mu-r-code-card-code-tracking);\n  color: var(--mu-r-code-card-code-ink);\n}\n.mu-codecard-code .mu-code-ln { display: inline-block; width: var(--mu-r-code-card-code-number); color: var(--mu-r-code-card-tint-line); }\n.mu-codecard-code .mu-code-kw { color: var(--mu-r-code-card-tint-keyword); }\n.mu-codecard-code .mu-code-ty { color: var(--mu-r-code-card-tint-type); }\n.mu-codecard-code .mu-code-st { color: var(--mu-r-code-card-tint-string); }\n.mu-codecard-code .mu-code-cm { color: var(--mu-r-code-card-tint-comment); }\n.mu-codecard-code .mu-code-nu { color: var(--mu-r-code-card-tint-number); }\n"
    }
  ],
  "docs": "Agent guide: https://metalui.dev/r/code-card.md. SwiftUI: MetalCodeCard in the MetalUI Swift package."
}
